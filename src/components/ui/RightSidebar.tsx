"use client";

/*
import Link from "next/link";
import { useSession } from "next-auth/react";
*/
export default function RightSidebar() {

  return null;
  /*  
  const { data: session, status } = useSession();

    return (
      session ? 
      (
        <aside className="w-64 h-full bg-gray-100 border-l border-gray-300 p-4">
            <nav className="space-y-2">
            <Link href="/" className="block p-2 rounded bg-blue-100">Inicio</Link>
            <Link href="/protected/aicg" className="block p-2 rounded hover:bg-gray-200">Content Genrator</Link>
            <Link href="/protected/testpage" className="block p-2 rounded hover:bg-gray-200">test</Link>
            </nav>
        </aside>
      )
      : null
    );*/
  }
  